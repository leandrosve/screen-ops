namespace Common.Enums
{
    public enum EntityStatus
    {
        Draft,       // Borrador (solo visible en CMS/backend, editable)
        Published,   // Publicado (visible al público)
        Hidden,      // Oculto (no visible)
        Deleted
    }
}
